SUBDIRS = doc src

bindist:
	$(SHELL) @BINDIST_SCRIPT@ $(STRIP) $(top_srcdir) nozip
bindistzip:
	$(SHELL) @BINDIST_SCRIPT@ $(STRIP) $(top_srcdir) zip

EXTRA_DIST = AUTHORS CODE_OF_CONDUCT COPYING HACKING INSTALL NEWS PHILOSOPHY README TODO .gitignore
